# Training schedule

See [training_stages.md](training_stages.md) for the current `STAGE_SCHEDULE`.

Expression deformer is **off in stage 1**, on in **stage 2** (`2_expression_detail`).

Losses are the same basic stack on stages 0–3 (no `w_seg` / expression-gap terms).
